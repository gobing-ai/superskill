---
schema_version: 1
id: "A"
name: "Portable plugin scripts via install-time staging"
status: backlog
priority: P2
tags: []
created_at: "2026-07-17T06:13:24.436Z"
updated_at: "2026-07-17T06:15:28.926Z"
---

# A: Portable plugin scripts via install-time staging

## Goal
Make plugin-level `scripts/` a first-class install artifact with a documented, portable invocation contract so any plugin can ship skill-related scripts without one-off absorption into the CLI.
## Scope
- **In:** Install-time staging of `plugins/<plugin>/scripts/**`; `superskill script path` helper; dual contract (path standard + optional `script run` registry); guide rewrite under `docs/help/`; non-hook `validate-response` migration; hook-path unification design (R6-B); ADR/doc supersession for ADR-015 / ADR-022 / `04_DESIGN` / `bundled_plugin`.
- **Out:** Promoting scripts to a workspace package; runtime discovery of arbitrary third-party scripts without install; requiring Bun on all targets; deleting hook emitters before a replacement design lands.
## Acceptance Criteria

```gherkin
Feature: Portable plugin scripts via install-time staging

  Scenario: Basic acceptance
    Given a precondition
    When an action
    Then an expected outcome
```

## Tasks

<!-- AUTO-GENERATED by spur feature refresh -->
<!-- END AUTO-GENERATED -->

## Notes
### Domain context
- Layout convention (kept): `plugins/<plugin>/scripts/<feature>/` shared libs — NOT per-skill `scripts/` (ADR-015).
- Today: mapper copies only skill-level support subdirs (`packages/core/src/mapper.ts:153-158`); plugin-level `scripts/` never stages for rulesync targets.
- Claude/OMP/Grok native install already receives full plugin trees (including `scripts/`); rulesync/hermes do not.
- Shipped absorption path (task 0087): `superskill script run` + `ScriptRunner` in `apps/cli/src/commands/script-run.ts` — reopened under redesign; dual contract keeps it **optional**.
- Hooks today: PATH form `superskill hook run <plugin> <id>` (portable); operator locked R6-B to move hooks toward staged paths — high regression risk.

### Skills every session should consult
- `sp-spur-cli` for task/feature updates
- `sp-source-driven-development` before claiming install/mapper API facts
- `sp-spur-dev` / `sp-dev-refine` when a ticket is ready to execute
- Guide: `docs/help/how_to_organize_scripts_for_plugin_development.md` (will be rewritten)
- ADRs: `docs/00_ADR.md` ADR-015, ADR-020–022; install help `docs/help/cmd_install.md`

### Standing preferences (locked discovery)
| ID | Decision |
|----|----------|
| C | Full reopen; redesign around install-time copy (not absorption-only) |
| R2-B | Portable runner contract after copy (not require Bun everywhere) |
| R3-B | Native plugin `scripts/` for Claude/OMP/Grok + rulesync `~/.agents/scripts/<plugin>/` |
| R4-B | Skill docs use superskill path helper (never hardcode cache/repo paths) |
| R5-B | Dual contract: path = standard; `script run` registry = optional |
| R6-B | Hooks also invoke staged script paths (unify; largest risk) |
| R7-B | 0087 first-slice umbrella + sibling tickets; not one mega-task |

### Reopened work
- **0087** reopened `done → wip → blocked` pending **0088** + **0089**. Original absorption Solution retained for history; Requirements/Design must be rewritten after frontier research tickets resolve — do not re-verify absorption as the end state.

## Destination

Every agent-skill-related plugin script is delivered by `superskill install` staging (native plugin tree or `~/.agents/scripts/<plugin>/`) and invocable on every supported target through a superskill-resolved path under a portable runner contract — not repo-relative `bun` paths and not CLI absorption alone.

## Decisions so far

<!-- Populated as tickets resolve: `- [WBS title](path) — one-line gist` -->
- (charting session) Discovery locked C + R2-B…R7-B — see Notes.
- [0088 Inventory real installed script paths](../tasks/0088_inventory-real-installed-script-paths-per-target-claude-omp-.md) — native targets (claude/omp/grok) already land full plugin trees incl. `scripts/`; rulesync+surrogate targets (codex/pi/opencode/antigravity-*/hermes) get skill dirs only — plugin-level `scripts/` absent. Mapper (`mapper.ts:153-158`) copies skill-level subdirs only; staging seam for the rulesync class is task 0090.
- [0089 Define portable entrypoint contract](../tasks/0089_define-portable-entrypoint-contract-for-staged-plugin-script.md) — Entrypoint Contract v1: Node `.js`/`.mjs` + POSIX `.sh` as portable runtimes (no Bun on target); `$(superskill script path <p> <id>)` as canonical doc form; exit 0/1 for validation CLIs vs exit 2 for hook blocks; path is standard surface, `script run` optional legacy.
- [0090 Stage plugin-level scripts on install](../tasks/0090_stage-plugin-level-scripts-on-superskill-install-mapper-disp.md) — mapper stages `plugins/<p>/scripts/` → `.rulesync/scripts/<p>/`; install dispatches to `~/.agents/scripts/<p>/` (global) or project twin for rulesync+hermes; native targets (claude/omp/grok) skip (already receive scripts via plugin install CLI). Tree shape preserved, re-install safe per-plugin subdir.
- [0091 Add script path helper](../tasks/0091_add-superskill-script-path-helper-resolving-staged-script-lo.md) — `superskill script path <p> <rel>` resolves staged entrypoints: project `.agents/scripts/` first, then global `~/.agents/scripts/`; exit 0 found, exit 2 not-found (fail-closed), exit 1 invalid args; `--json` machine output; `--global`/`--project` flags; path traversal rejected.
- [0092 Rewrite plugin-scripts guide](../tasks/0092_rewrite-plugin-scripts-guide-for-install-staging-dual-c.md) — Guide rewritten for the dual contract: standard staged path (`script path` + portable Node/sh runner per Entrypoint Contract) vs optional binary registry (`script run`/`hook run`); install staging roots, exit codes, revised anti-patterns (staging is intended, ad-hoc/repo-relative copies still banned), Status honesty (hook-path unification Planned not Shipped). Help index + bundled_plugin one-liner aligned; CHANGELOG docs note added.
- [0093 Migrate non-hook validate-response docs](../tasks/0093_migrate-non-hook-validate-response-docs-to-path-based-invoca.md) — Anti-hallucination non-hook docs + README teach dual contract: standard `"$(superskill script path cc anti-hallucination/validate_response.js)"` first, optional `script run cc validate-response` retained; no `bun plugins/cc/scripts` in skill docs; interim honesty note until portable `.js` twin ships.
- [0094 Design hook-path unification without CLAUDE_PLUGIN_ROOT regression](../tasks/0094_design-hook-path-unification-without-claude-plugin-root-regr.md) — keep `superskill hook run` as the sole hooks.json form; R6-B narrowed to "unify delivery (install staging), not runtime invocation form." Zero emitter changes; minCliVersion role preserved; ADR-020 fail-open intact. Proposed follow-up 0096 (`.js` twin for validate_response.ts/ah_guard.ts).

## Not yet specified

- Exact resolution order for `script path` when both native plugin root and `~/.agents/scripts/` exist (multi-target machine).
- Whether staged entrypoints are Node-only, shell-only, or allow a small set of compiled JS emitted at install.
- How hermes/omp/pi emitters rewrite hook commands if hooks leave `hook run` (R6-B).
- Whether third-party marketplace plugins (not in this monorepo) get the same staging without a CLI release.
- Interaction with `minCliVersion` if hooks stop calling the CLI.
- Project-mode (`--no-global`) twin path conventions for scripts root.
- Whether to deprecate/remove `script run` after path is standard, or keep forever for pure in-binary validators.
- Coverage/gate impact of staging TS tests under new install destinations.
- Whether Claude marketplace sync (outside `superskill install`) still needs a separate story for scripts.

## Out of scope

- Per-skill `skills/<name>/scripts/` executable revival (ADR-015 anti-pattern).
- Promoting guard engines to `@gobing-ai/*` workspace packages (rejected by ADR-022 ownership model unless a later ticket reopens packaging).
- Requiring Bun as a hard dependency on all agent hosts.
- Cross-repo Spur workflow Phase 4 anti-hallucination launcher work (task 0041) — separate effort.
- Non-script install features (MCP, magents) beyond what staging hooks into.

## History
